#include <stdio.h>

/**
 * main - This is the entry point of the code.
 * Return: This will return 0 if the code runs successfully.
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
